streamlit>=1.31
transformers>=4.42.0
peft>=0.11.0
accelerate>=0.30.0
safetensors>=0.4.2
# Optional: only used if GPU + CUDA available for 4-bit quantization
bitsandbytes>=0.43.0
# Choose appropriate torch based on your platform (GPU vs CPU build)
torch>=2.1.0
